/// <reference types="Cypress" />
/// <reference types="@testing-library/cypress" />

describe("unauthenticated users", () => {
  
  context("index page", () => {
    it("should have a login link visible in the navbar", () => {});
    
    it("should see <= 10 posts", () => {});
    
    it("should not see a new post submit form", () => {});
    
    it("should not be able to like posts", () => {});
    
    it("should not be able to edit posts", () => {});
  });
  
  context("profile page", () => {
    it("should have a login link visible in the navbar", () => {});
    
    it("should see <= 10 posts", () => {});

    it("should only show posts from that user", () => {})

    it("should not be able to like posts", () => {});

    it("should not be able to edit posts", () => {});

    it("should not be able to follow users", () => {});
  });


});
